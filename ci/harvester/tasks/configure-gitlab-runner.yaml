---
## Configure this VM's GitLab runners — one runner entry per role it hosts.
## Config location: ~/.gitlab-runner/config.toml (user-level)
## Managed by: systemd (running as ubuntu user)

- name: Set variables
  ansible.builtin.set_fact:
    runner_user: "{{ ansible_user }}"
    runner_home: "/home/{{ ansible_user }}"
    runner_config_dir: "/home/{{ ansible_user }}/.gitlab-runner"
    runner_config_path: "/home/{{ ansible_user }}/.gitlab-runner/config.toml"
  become: no

- name: Resolve this VM's runner definitions
  ansible.builtin.set_fact:
    resolved_runners: >-
      {{
        resolved_runners | default([]) + [
          runner_defaults
          | combine(runner_roles[item])
          | combine(runner_overrides[item] | default({}))
          | combine({'role': item,
                     'description': item ~ '-runner-' ~ vm_name})
        ]
      }}
  loop: "{{ runners }}"
  become: no

# One job slot per runner limit — anything lower silently caps the roles.
- name: Compute global concurrency for this VM
  ansible.builtin.set_fact:
    runner_global_concurrency: "{{ resolved_runners | map(attribute='limit') | map('int') | sum }}"
  become: no

- name: Ensure .gitlab-runner directory exists
  ansible.builtin.file:
    path: "{{ runner_config_dir }}"
    state: directory
    mode: "0700"
  become: no

- name: Install GitLab Runner package
  block:
    - name: Download GitLab Runner repository setup script
      ansible.builtin.get_url:
        url: "https://packages.gitlab.com/install/repositories/runner/gitlab-runner/script.deb.sh"
        dest: /tmp/gitlab-runner-script.deb.sh
        mode: "0755"

    - name: Add GitLab apt repository
      ansible.builtin.command: bash /tmp/gitlab-runner-script.deb.sh
      args:
        creates: /etc/apt/sources.list.d/runner_gitlab-runner.list
      become: yes

    - name: Mask system-wide gitlab-runner service before install
      ansible.builtin.file:
        src: /dev/null
        dest: /etc/systemd/system/gitlab-runner.service
        state: link
        force: yes
      become: yes
    - name: Install gitlab-runner package
      ansible.builtin.apt:
        name: gitlab-runner
        state: latest
        update_cache: yes
      become: yes

- name: Configure GitLab Runner user-mode service
  become: no
  block:
    - name: Stop and disable system-wide gitlab-runner service
      ansible.builtin.systemd:
        name: gitlab-runner
        state: stopped
        enabled: no
      become: yes

    - name: Ensure systemd user directory exists
      ansible.builtin.file:
        path: "{{ runner_home }}/.config/systemd/user"
        state: directory
        mode: "0700"

    - name: Create gitlab-runner user-mode systemd service
      ansible.builtin.copy:
        dest: "{{ runner_home }}/.config/systemd/user/gitlab-runner.service"
        mode: "0644"
        content: |
          [Unit]
          Description=GitLab Runner (User Mode)
          After=network.target

          [Service]
          Type=simple
          ExecStart=/usr/bin/gitlab-runner run
          WorkingDirectory={{ runner_home }}
          Restart=always
          RestartSec=5

          [Install]
          WantedBy=default.target

    - name: Enable lingering so user services run without login
      become: yes
      ansible.builtin.command: loginctl enable-linger {{ runner_user }}

    - name: Reload systemd user manager
      ansible.builtin.command: systemctl --user daemon-reload

    - name: Enable and start user-mode gitlab-runner
      ansible.builtin.command: systemctl --user enable --now gitlab-runner

# Rebuilt from scratch below, so roles dropped from the inventory disappear
# from the VM instead of lingering as orphaned runner entries.
- name: Remove existing GitLab Runner config.toml
  ansible.builtin.file:
    path: "{{ runner_config_path }}"
    state: absent
  become: no

- name: Copy config.py to runner home
  ansible.builtin.copy:
    src: config.py
    dest: "{{ runner_home }}/config.py"
    mode: "0755"

################################################
- name: Register each runner role
  ansible.builtin.include_tasks: register-gitlab-runner.yaml
  loop: "{{ resolved_runners }}"
  loop_control:
    loop_var: runner
    label: "{{ runner.description }}"

################################################
# The config was rewritten out from under the running process; a reload picks
# up every entry at once.
- name: Restart user-mode gitlab-runner
  ansible.builtin.command: systemctl --user restart gitlab-runner
  become: no

- name: Verify GitLab runners
  become: no
  block:
    - name: Check runner status
      ansible.builtin.command: gitlab-runner status
      register: runner_status
      changed_when: false
      failed_when: false

    - name: Verify runner connectivity
      ansible.builtin.command: gitlab-runner verify
      register: runner_verify
      changed_when: false
      failed_when: false

- name: Check if .bash_logout exists for runner user
  ansible.builtin.stat:
    path: "{{ runner_home }}/.bash_logout"
  register: bash_logout
  become: yes
  become_user: "{{ ansible_user }}"

- name: Remove .bash_logout file which might conflict with the GitLab runner
  ansible.builtin.file:
    path: "{{ runner_home }}/.bash_logout"
    state: absent
  when: bash_logout.stat.exists
  become: yes
  become_user: "{{ ansible_user }}"
