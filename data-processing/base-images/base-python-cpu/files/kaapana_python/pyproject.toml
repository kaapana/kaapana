[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kaapanapy"
version = "0.0.1"
dependencies = [
    "opensearch-py==2.4.2",
    "pydantic==1.10.14"
]
