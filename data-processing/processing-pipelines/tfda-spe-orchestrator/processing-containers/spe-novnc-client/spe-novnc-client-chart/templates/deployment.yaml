kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Release.Name }}
  namespace: "{{ tpl .Values.global.namespace . }}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app-name: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app-name: {{ .Release.Name }}
    spec:
      restartPolicy: Always
      containers:
      - name: spe-novnc-client
        image: "{{  .Values.global.registry_url }}/spe-novnc-client:{{ .Values.global.kaapana_build_version  }}"
        imagePullPolicy: {{ .Values.global.pull_policy_images }}
        resources:
          requests:
            memory: "100Mi" # this could be setup according to requested resources
          limits:
            memory: "500Mi"
        env:
        - name: INGRESS_PATH
          value: {{ printf "/%s" .Release.Name }}
        - name: WORKFLOW_DIR
          value: "{{ .Values.workflow_dir }}"
        - name: BATCH_NAME
          value: "{{ .Values.batch_name }}"
        - name: OPERATOR_OUT_DIR
          value: "{{ .Values.operator_out_dir }}" 
        - name: OPERATOR_IN_DIR
          value: "{{ .Values.operator_in_dir }}"
        - name: AUTOCONNECT
          value: "{{ .Values.global.AUTOCONNECT }}"
        - name: VNC_SERVER
          value: "{{ .Values.global.VNC_SERVER }}"
        - name: VNC_PASSWORD
          value: "{{ .Values.global.VNC_PASSWORD }}"
        - name: VIEW_ONLY
          value: "{{ .Values.global.VIEW_ONLY }}"
        ports:
          - containerPort: 6080
        volumeMounts:
{{ include "dynamicVolumeMounts" $ | indent 10 }}
      volumes:
{{ include "dynamicVolumes" $ | indent 6 }}
      imagePullSecrets:
      - name: registry-secret
