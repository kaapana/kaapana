---
global:
  registry_url: ""
  pull_policy_images: ""
  base_namespace: ""
  fast_data_dir: ""
  slow_data_dir: ""
  namespace: "{{ .Values.global.jobs_namespace }}"
  dynamicVolumes:
  port: 6080
  AUTOCONNECT: "true"
  VNC_SERVER: "<ip_address>:5901" # TODO: this needs to be dynamically assigned from ip addr file in the mounted DAG folder
  VNC_PASSWORD: "your_vnc_password" # TODO: this needs to be dynamically assigned from ip addr file in the mounted DAG folder
  VIEW_ONLY: "false"
