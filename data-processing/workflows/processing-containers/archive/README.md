# archive

File archiving. Replaces `ZipUnzipOperator`.

## Templates needed

- `zip`: pack files into a zip archive with whitelist/blacklist patterns, optional subdir, and a batch-level toggle.
- `unzip`: extract zip archives.
