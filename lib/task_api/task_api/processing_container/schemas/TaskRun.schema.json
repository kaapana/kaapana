{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Task Run",
  "description": "Execution record of a TaskInstance. Includes runtime metadata to track and identify the actual container or pod running the task.",
  "allOf": [
    {"$ref": "TaskInstance.schema.json"},
    {
      "type": "object",
      "required": [
        "id",
        "mode"
      ],
      "properties": {
        "id": {
        "type": "string"
        },
        "mode": {
          "type": "string"
        }
      }
    }
  ]
}