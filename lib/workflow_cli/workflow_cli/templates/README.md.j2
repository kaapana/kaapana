# {{ workflow_display_name }}

{{ workflow_engine | upper }} workflow created on {{ date }}.

## Description

Add your workflow description here.

## Usage

1. Edit `workflow-chart/files/workflow.json` to configure parameters
2. Edit `workflow-chart/files/workflow_definition.*` with your workflow logic
3. Validate: `workflow-cli validate .`
4. Package: `workflow-cli package .`
5. Install: `helm install {{ workflow_name }} workflow-chart/build/{{ workflow_name }}-0.0.0.tgz`

## Structure

```
{{ workflow_name }}/
├── workflow-chart/
│   ├── Chart.yaml              # Helm chart metadata
│   ├── values.yaml             # Configuration values
│   ├── files/
│   │   ├── workflow.json       # Workflow metadata & parameters
│   │   └── workflow_definition.*  # Workflow code
│   └── templates/
│       ├── configmap.yaml      # Mounts workflow files
│       └── job.yaml            # Installer job
└── README.md
```

## Development

```bash
# Validate
workflow-cli validate .

# Package
workflow-cli package .
```
