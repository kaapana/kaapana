---
{{- /* Toggleable via global.enable_privileged_pod_admission_policy so
deployments can disable the policy without switching to dev_mode.
NOTE: compared as lower-cased string because `| default true` would swallow
an explicit `false` (Helm treats false as empty). Unset still enforces. */ -}}
{{- if and (not .Values.global.dev_mode) (ne (lower (toString .Values.global.enable_privileged_pod_admission_policy)) "false") }}
apiVersion: admissionregistration.k8s.io/v1beta1
kind: ValidatingAdmissionPolicy
metadata:
  name: "forbid-privileged-pods"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:   [""]
      apiVersions: ["v1"]
      operations:  ["CREATE", "UPDATE"]
      resources:   ["pods"]
  variables:
    - name: containersWithSecurityContext
      expression: "object.spec.containers.filter(c, has(c.securityContext) && has(c.securityContext.privileged))"
  validations:
    - expression: "variables.containersWithSecurityContext.all(c, c.securityContext.privileged != true)"
---
apiVersion: admissionregistration.k8s.io/v1beta1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "forbid-privileged-pods-binding"
spec:
  policyName: "forbid-privileged-pods"
  validationActions: [Deny]
  matchResources:
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values:
          - "kube-system"
{{- end }}