apiVersion: v1
kind: Service
metadata:
  name: cbioportal-session-service
  namespace: "services"
  labels:
    app-name: cbioportal-session
spec:
  selector:
    app-name: cbioportal-session
  ports:
    - port: 5000
      targetPort: 5000