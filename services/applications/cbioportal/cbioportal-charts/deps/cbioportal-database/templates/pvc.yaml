apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cbio-mysql-pv-claim
  namespace: "services"
spec:
  storageClassName: "host-dir" 
  volumeName: cbio-mysql-pv-volume
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "10Gi"