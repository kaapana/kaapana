apiVersion: v1
kind: Service
metadata:
  name: cbioportal-database
  namespace: "services"
  labels:
    app-name: cbioportal-database
spec:
  selector:
    app-name: cbioportal-database
  ports:
    - port: 3306
      targetPort: 3306
# spec:
#   selector:
#     app-name: cbioportal-database
#   ports:
#     - port: 3306
#       targetPort: 33060
#       nodePort: 3306 # only for development!

  type: ClusterIP
  # type: NodePort