apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix-cbioportal
  namespace: "services"
spec:
  stripPrefix:
    prefixes:
    - /cbioportal-webapp
    forceSlash: false