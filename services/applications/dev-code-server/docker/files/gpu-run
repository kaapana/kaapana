#!/bin/sh
set -eu

# (optional) validate required env
: "${GPU_JOB_NAMESPACE:?GPU_JOB_NAMESPACE must be set}"
: "${RELEASE_NAME:?RELEASE_NAME must be set}"

# Call the privileged command; args are forwarded
exec sudo /usr/local/bin/gpu-run-root "$@"
