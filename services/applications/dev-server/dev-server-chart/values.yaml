---
global:
  ingress_path: ""
  complete_image: "{{ .Values.global.registry_url }}/code-server:{{ .Values.global.kaapana_build_version  }}"
  namespace: ""
  envVars:
  - name: WORKSPACE
    value: "/kaapana"