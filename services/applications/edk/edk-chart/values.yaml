---
global:
  ingress_path: "edk"
  complete_image: "{{ .Values.global.registry_url }}/edk:{{ .Values.global.kaapana_build_version  }}"
  namespace: "{{ .Values.global.services_namespace }}"
  dynamicVolumes:
  - name: edk-data
    mount_path: /kaapana/mounted/extensions
    host_path: /extensions
    