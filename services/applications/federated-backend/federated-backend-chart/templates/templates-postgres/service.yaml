
apiVersion: v1
kind: Service
metadata:
  name: federated-postgres-service
  namespace: base
  #namespace: {{ .Values.global.base_namespace }}
  labels:
    app-name: federated-postgres
spec:
  selector:
    app-name: federated-postgres
  ports:
    - port: 5432
      targetPort: 5432