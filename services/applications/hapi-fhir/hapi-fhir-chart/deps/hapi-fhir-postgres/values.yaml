---
global:
  namespace: "{{ .Values.global.services_namespace }}"

  dynamicVolumes:
    - name: hapi-fhir-postgres-data
      mount_path: /var/lib/postgresql/data
      host_path: /hapi-fhir-postgres-data
      storage: "5Gi"
