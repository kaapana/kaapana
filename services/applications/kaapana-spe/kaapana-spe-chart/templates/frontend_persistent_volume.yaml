# dev-files
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: spe-dev-frontend-pv-volume
  labels:
    type: local
spec:
  capacity:
    storage: "500Mi"
  storageClassName: host-dir
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ .Values.global.frontend_dev_files }}"
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: "{{ .Values.global.services_namespace }}"
    name: spe-dev-frontend-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spe-dev-frontend-pv-claim
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "500Mi"
  volumeName: "spe-dev-frontend-pv-volume"
