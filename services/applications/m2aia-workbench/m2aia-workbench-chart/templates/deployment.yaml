{{- $uuid := uuidv4 | trunc 10 }}
kind: Deployment
apiVersion: apps/v1
metadata:
  name: m2aia-vol-{{ $uuid }}
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app-name: m2aia-vol-{{ $uuid }}
  template:
    metadata:
      labels:
        app-name: m2aia-vol-{{ $uuid }}
    spec:
      restartPolicy: Always
      containers:
        # ToDo add init-container to wait for db-host
      - name: m2aia
        image: "{{ .Values.global.registry_url }}/m2aia-workbench:{{ .Values.global.kaapana_build_version  }}"
        imagePullPolicy: {{ .Values.global.pull_policy_pods }}
        resources:
          requests:
            memory: "5Gi"
          limits:
            memory: "8Gi"
        env:
        - name: INGRESS_PATH
          value: "m2aia-vol-{{ $uuid }}"
        volumeMounts:
        - name: m2aia-data
          mountPath: /data
      volumes:
      - name: m2aia-data
        hostPath:
          {{- if .Values.mount_path }}
          path: {{ .Values.mount_path }}
          {{- else }}
          path: {{ .Values.global.slow_data_dir }}/minio
          {{- end }}
          type: DirectoryOrCreate
      imagePullSecrets:
      - name: registry-secret
---
kind: Service
apiVersion: v1
metadata:
  name: m2aia-vol-{{ $uuid }}
  namespace: "{{ .Values.global.services_namespace }}"
  labels:
    app: m2aia-vol-{{ $uuid }}
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: pt-m2aia-http
  type: ClusterIP
  selector:
    app-name: m2aia-vol-{{ $uuid }}
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: m2aia-vol-{{ $uuid }}-redirect
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  redirectRegex:
    regex: ^.*/m2aia-vol-{{ $uuid }}([^\/]*)$
    replacement: /m2aia-vol-{{ $uuid }}/
---
apiVersion: networking.k8s.io/v1 
kind: Ingress
metadata:
  name: m2aia-vol-{{ $uuid }}
  namespace: "{{ .Values.global.services_namespace }}"
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: {{  .Values.global.services_namespace  }}-auth-check@kubernetescrd,{{ .Values.global.services_namespace }}-m2aia-vol-{{ $uuid }}-redirect@kubernetescrd
    kubernetes.io/ingress.global-static-ip-name: "kubernetes-ingress"
spec:
  rules:
  - host:
    http:
      paths:
      - path: /m2aia-vol-{{ $uuid }}
        pathType: ImplementationSpecific
        backend:
          service:
            name: m2aia-vol-{{ $uuid }}
            port:
              name: pt-m2aia-http
---

          
