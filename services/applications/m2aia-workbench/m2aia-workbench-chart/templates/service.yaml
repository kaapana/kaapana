{{- $namespace := .Values.global.services_namespace | toString }}
---
kind: Service
apiVersion: v1
metadata:
  name: m2aia-vol-{{ .Values.global.uuid }}
  namespace: "{{ $namespace }}"
  labels:
    app: m2aia-vol-{{ .Values.global.uuid }}
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: pt-m2aia-http
  type: ClusterIP
  selector:
    app-name: m2aia-vol-{{ .Values.global.uuid }}
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: m2aia-vol-{{ .Values.global.uuid }}-redirect
  namespace: "{{ $namespace }}"
spec:
  redirectRegex:
    regex: ^.*/m2aia-vol-{{ .Values.global.uuid }}([^\/]*)$
    replacement: /m2aia-vol-{{ .Values.global.uuid }}/
---
apiVersion: networking.k8s.io/v1 
kind: Ingress
metadata:
  name: m2aia-vol-{{ .Values.global.uuid }}
  namespace: "{{ $namespace }}"
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: {{  .Values.global.services_namespace  }}-auth-check@kubernetescrd,{{ $namespace }}-m2aia-vol-{{ .Values.global.uuid }}-redirect@kubernetescrd
    kubernetes.io/ingress.global-static-ip-name: "kubernetes-ingress"
spec:
  rules:
  - host:
    http:
      paths:
      - path: /m2aia-vol-{{ .Values.global.uuid }}
        pathType: ImplementationSpecific
        backend:
          service:
            name: m2aia-vol-{{ .Values.global.uuid }}
            port:
              name: pt-m2aia-http
---