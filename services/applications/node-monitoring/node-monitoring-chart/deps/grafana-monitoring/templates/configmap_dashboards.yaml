---
apiVersion: v1
data:
{{ (.Files.Glob "files/racoon-dashboard.json").AsConfig | indent 2 }}
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: "{{ .Values.global.extensions_namespace }}"
