---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: "{{ .Values.global.extensions_namespace }}"
  labels:
    app-name: grafana
spec:
  selector:
    app-name: grafana
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: gf-strip-prefix
  namespace: "{{  .Values.global.extensions_namespace  }}"
spec:
  stripPrefix:
    prefixes:
    - /racoon-monitoring
    forceSlash: true
---
apiVersion: networking.k8s.io/v1 
kind: Ingress
metadata:
  name: grafna-ingress
  namespace: "{{ .Values.global.extensions_namespace }}"
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: {{  .Values.global.extensions_namespace  }}-gf-strip-prefix@kubernetescrd
    kubernetes.io/ingress.global-static-ip-name: "kubernetes-ingress"
spec:
  rules:
  - host: 
    http:
      paths:
      - path: /racoon-monitoring
        pathType: ImplementationSpecific
        backend:
          service:
            name: grafana
            port:
              number: 3000
---