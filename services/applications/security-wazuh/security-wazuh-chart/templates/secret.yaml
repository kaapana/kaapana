apiVersion: v1
kind: Secret
metadata:
  name: wazuh-api-credentials
  namespace: "{{ .Values.global.security_namespace }}"
type: Opaque
stringData:
  username: wazuh-wui
  password: "{{ randAlphaNum 32 }}aU5_" # append additional characters to ensure wazuh password policy (at least one uppercase and one lowercase letter, a number, and a symbol) is fulfilled enery time