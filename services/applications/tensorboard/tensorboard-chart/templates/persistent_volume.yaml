# Dags
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: tb-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: "50Mi"
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ .Values.global.fast_data_dir }}/tensorboard"
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: "{{ .Values.global.services_namespace }}"
    name: tb-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tb-pv-claim
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "50Mi"
  volumeName: "tb-pv-volume"
---
