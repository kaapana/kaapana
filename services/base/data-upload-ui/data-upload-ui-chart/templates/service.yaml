---
apiVersion: v1
kind: Service
metadata:
  name: data-upload-ui-service
  namespace: "{{ .Values.global.services_namespace }}"
  labels:
    app.kubernetes.io/name: data-upload-ui-service
spec:
  selector:
    app.kubernetes.io/name: data-upload-ui
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix-data-upload-ui
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  stripPrefix:
    prefixes:
    - /data-upload-ui
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: data-upload-ui-ingress
  namespace: "{{ .Values.global.services_namespace }}"
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Values.global.services_namespace }}-strip-prefix-data-upload-ui@kubernetescrd
    kubernetes.io/ingress.global-static-ip-name: "kubernetes-ingress"
    # kaapana.ai/ui.* discovery annotations — schema owned by menu-discovery work
    kaapana.ai/ui.name: "Data Upload"
    kaapana.ai/ui.section: "workflows"
    kaapana.ai/ui.icon: "mdi-cloud-upload"
    kaapana.ai/ui.order: "10"
    kaapana.ai/ui.id: "data-upload"
    kaapana.ai/ui.project: "path"
    kaapana.ai/ui.section-label: "Workflows"
    kaapana.ai/ui.section-icon: "mdi-gamepad-variant"
    kaapana.ai/ui.section-order: "10"
    kaapana.ai/ui.dev-links: "Kaapana Backend=/kaapana-backend/docs,AII=/aii/docs"
spec:
  rules:
  - host:
    http:
      paths:
      - path: /data-upload-ui
        pathType: ImplementationSpecific
        backend:
          service:
            name: data-upload-ui-service
            port:
              number: 5000
---
# Project-scoped route (/project/<id>/data-upload-ui/...): auth-backend
# authorizes against the <id>, the middlewares strip both prefixes, and the
# app derives its project scope from the document URL (see
# the shared selectedProject util in @kaapana/base-ui).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: data-upload-ui-project-ingressroute
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: PathRegexp(`^/project/[^/]+/data-upload-ui(/|$)`)
      middlewares:
        - name: strip-project-prefix
        - name: strip-prefix-data-upload-ui
      services:
        - name: data-upload-ui-service
          port: 5000
