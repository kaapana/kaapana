---
apiVersion: v1
kind: Service
metadata:
  name: federated-ui-service
  namespace: "{{ .Values.global.services_namespace }}"
  labels:
    app.kubernetes.io/name: federated-ui-service
spec:
  selector:
    app.kubernetes.io/name: federated-ui
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix-federated-ui
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  stripPrefix:
    prefixes:
    - /federated-ui
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: federated-ui-ingress
  namespace: "{{ .Values.global.services_namespace }}"
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Values.global.services_namespace }}-strip-prefix-federated-ui@kubernetescrd
    kubernetes.io/ingress.global-static-ip-name: "kubernetes-ingress"
    # kaapana.ai/ui.* discovery annotations — schema owned by menu-discovery work
    kaapana.ai/ui.name: "Instance Overview"
    kaapana.ai/ui.section: "workflows"
    kaapana.ai/ui.icon: "mdi-vector-triangle"
    kaapana.ai/ui.order: "60"
    kaapana.ai/ui.id: "runner-instances"
    kaapana.ai/ui.project: "path"
    kaapana.ai/ui.dev-links: "Kaapana Backend=/kaapana-backend/docs"
spec:
  rules:
  - host:
    http:
      paths:
      - path: /federated-ui
        pathType: ImplementationSpecific
        backend:
          service:
            name: federated-ui-service
            port:
              number: 5000
---
# Project-scoped route (/project/<id>/federated-ui/...): auth-backend
# authorizes against the <id>, the middlewares strip both prefixes, and the
# app derives its project scope from the document URL (see
# the shared selectedProject util in @kaapana/base-ui).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: federated-ui-project-ingressroute
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: PathRegexp(`^/project/[^/]+/federated-ui(/|$)`)
      middlewares:
        - name: strip-project-prefix
        - name: strip-prefix-federated-ui
      services:
        - name: federated-ui-service
          port: 5000
