---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix-portal-api
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  stripPrefix:
    prefixes:
    - /portal-api
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portal-api-ingress
  namespace: "{{ .Values.global.services_namespace }}"
  # deliberately no kaapana.ai/ui.* annotations: the discovery API itself is not a menu entry
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: {{  .Values.global.services_namespace  }}-strip-prefix-portal-api@kubernetescrd
    kubernetes.io/ingress.global-static-ip-name: "kubernetes-ingress"
spec:
  rules:
  - host:
    http:
      paths:
      - path: /portal-api
        pathType: Prefix
        backend:
          service:
            name: portal-api
            port:
              number: 80
---
apiVersion: v1
kind: Service
metadata:
  name: portal-api
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  selector:
    app.kubernetes.io/name: portal-api
    app.kubernetes.io/component: api
  ports:
    - port: 80
      targetPort: 8080
