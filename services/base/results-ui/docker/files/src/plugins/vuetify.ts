import { createKaapanaVuetify } from '@kaapana/base-ui'

// The shared factory carries the Kaapana themes, the mdi icon set and the
// platform typeface, so this view cannot ship the theme and forget the font.
export default createKaapanaVuetify()
