apiVersion: v1
kind: Service
metadata:
  name: aip-postgres-service
  namespace: "{{ .Values.global.services_namespace }}"
  labels: 
    app.kubernetes.io/name: aip-postgres
spec:
  selector: 
    app.kubernetes.io/name: access-information-point 
  ports:
    - port: 5432
      targetPort: 5432