apiVersion: v1
kind: Service
metadata:
  name: dicom-project-mapping-postgres-service
  namespace: "{{ .Values.global.services_namespace }}"
  labels: 
    app.kubernetes.io/name: dicom-project-mapping-postgres
spec:
  selector: 
    app.kubernetes.io/name: dicom-project-mapping-postgres
  ports:
    - port: 5432
      targetPort: 5432