---
apiVersion: v1
kind: Service
metadata:
  name: project-listing-ui-service
  namespace: "{{ .Values.global.services_namespace }}"
  labels: 
    app-name: project-listing-ui-service
spec:
  selector: 
    app-name: project-listing-ui-app
  ports:
    - name: proj-list-http-svc
      port: 80
      targetPort: proj-list-http
  type: ClusterIP
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix-proj-listing-ui
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  stripPrefix:
    prefixes:
    - /projects-ui
    forceSlash: true
---
apiVersion: networking.k8s.io/v1 
kind: Ingress
metadata:
  name: proj-listing-ui-ingress
  namespace: "{{ .Values.global.services_namespace }}"
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: {{  .Values.global.services_namespace  }}-strip-prefix-proj-listing-ui@kubernetescrd
    kubernetes.io/ingress.global-static-ip-name: "kubernetes-ingress"
spec:
  rules:
  - host: 
    http:
      paths:
      - path: /projects-ui
        pathType: ImplementationSpecific
        backend:
          service:
            name: project-listing-ui-service
            port:
              number: 80
---