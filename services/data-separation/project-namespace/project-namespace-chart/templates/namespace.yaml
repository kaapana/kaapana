---
apiVersion: v1
kind: Namespace
metadata:
  name: "{{ .Values.global.project_namespace | default (printf `%s-project-admin` (required `global.platform_prefix is required` .Values.global.platform_prefix)) }}"
  annotations:
    helm.sh/resource-policy: keep
