---
apiVersion: v1
kind: Namespace
metadata:
  name: "{{ .Values.global.project_namespace | default (printf `%s-project-admin` (.Values.global.platform_prefix | default `kaapana`)) }}"
  annotations:
    helm.sh/resource-policy: keep
