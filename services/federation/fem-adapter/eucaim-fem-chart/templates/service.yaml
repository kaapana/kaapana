---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-fem-adapter
  namespace: {{ .Values.global.services_namespace }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}-fem-adapter
  ports:
    - port: 8090
      targetPort: 8090
