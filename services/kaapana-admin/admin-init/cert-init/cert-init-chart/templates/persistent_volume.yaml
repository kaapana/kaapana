# Dags
apiVersion: v1
kind: PersistentVolume
metadata:
  name: tls-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: "10Mi"
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ .Values.global.fast_data_dir }}/tls"
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: "{{ .Values.global.admin_namespace }}"
    name: tls-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tls-pv-claim
  namespace: "{{ .Values.global.admin_namespace }}"
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "10Mi"
  volumeName: "tls-pv-volume"
---
