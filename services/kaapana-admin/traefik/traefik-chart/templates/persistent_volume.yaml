# Dags
apiVersion: v1
kind: PersistentVolume
metadata:
  name: treaefik-pv-volume
  labels:
    type: local
spec:
  storageClassName: nfs
  capacity:
    storage: "1Mi"
  accessModes:
    - ReadWriteMany
  nfs:
    server: "10.152.183.15"
    path: "/traefik"

  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: "{{ .Values.global.admin_namespace }}"
    name: treaefik-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: treaefik-pv-claim
  namespace: "{{ .Values.global.admin_namespace }}"
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "1Mi"
  volumeName: "treaefik-pv-volume"
---
