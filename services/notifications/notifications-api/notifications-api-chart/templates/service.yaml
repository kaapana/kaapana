apiVersion: v1
kind: Service
metadata:
  name: notifications-api-service
  namespace: "{{ .Values.global.services_namespace }}"
  labels: 
    app-name: notifications-api-service
spec:
  selector: 
    app-name: notifications-api 
  ports:
    - port: 8080
      targetPort: 8080