apiVersion: v1
kind: Secret
metadata:
  name: "notifications-postgres-password"
  namespace: "{{ .Values.global.services_namespace }}"
type: Opaque
stringData:
  notifications-postgres-password: {{ .Values.global.credentials_postgres_notifications_password }}
