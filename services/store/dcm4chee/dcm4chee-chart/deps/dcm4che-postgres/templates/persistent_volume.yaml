# Dags
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dcm4chee-postgres-pv-volume
  labels:
    type: local
spec:
  storageClassName: nfs
  capacity:
    storage: "200Mi"
  accessModes:
    - ReadWriteMany
  nfs:
    server: "10.152.183.15"
    path: "/postgres-dcm4che"

  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: "{{ .Values.global.services_namespace }}"
    name: dcm4chee-postgres-pv-claim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dcm4chee-postgres-pv-claim
  namespace: "{{ .Values.global.services_namespace }}"
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "200Mi"
  volumeName: "dcm4chee-postgres-pv-volume"
---
