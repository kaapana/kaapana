apiVersion: v1
kind: Secret
metadata:
  name: dicom-web-multiplexer-secret
  namespace: "{{ .Values.global.services_namespace }}"
type: Opaque
data:
  ae_title: "{{ .Values.global.ae_title | b64enc }}"
  dcmweb_endpoint: "{{ .Values.global.dcmweb_endpoint | b64enc }}"
  credentials.json: "{{ .Values.global.credentials | b64dec }}"
