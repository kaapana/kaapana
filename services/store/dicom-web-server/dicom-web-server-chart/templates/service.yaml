apiVersion: v1
kind: Service
metadata:
  name: dicom-web-server-service
  namespace: "{{ .Values.global.services_namespace }}"
  labels: 
    app-name: dicom-web-server-service
spec:
  selector: 
    app-name: dicom-web-server
  ports:
    - port: 8080
      targetPort: 8080