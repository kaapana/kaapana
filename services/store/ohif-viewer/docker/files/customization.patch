diff --git a/extensions/default/src/ViewerLayout/ViewerHeader.tsx b/extensions/default/src/ViewerLayout/ViewerHeader.tsx
index da9f989..642bd0c 100644
--- a/extensions/default/src/ViewerLayout/ViewerHeader.tsx
+++ b/extensions/default/src/ViewerLayout/ViewerHeader.tsx
@@ -99,7 +99,10 @@ function ViewerHeader({ appConfig }: withAppTypes<{ appConfig: AppTypes.Config }
   }
 
   return (
+    // The header stays mounted even when hidden: HeaderPatientInfo resolves
+    // patient data that the rest of the viewer reads.
     <Header
+      className={appConfig.showHeader ? undefined : 'hidden'}
       menuOptions={menuOptions}
       isReturnEnabled={!!appConfig.showStudyList}
       onClickReturnButton={onClickReturnButton}
diff --git a/extensions/default/src/ViewerLayout/index.tsx b/extensions/default/src/ViewerLayout/index.tsx
index 50533ff..0497129 100644
--- a/extensions/default/src/ViewerLayout/index.tsx
+++ b/extensions/default/src/ViewerLayout/index.tsx
@@ -159,13 +159,13 @@ function ViewerLayout({
       />
       <div
         className="relative flex w-full flex-row flex-nowrap items-stretch overflow-hidden bg-background"
-        style={{ height: 'calc(100vh - 52px)' }}
+        style={{ height: appConfig.showHeader ? 'calc(100vh - 52px)' : '100vh' }}
       >
         <React.Fragment>
           {showLoadingIndicator && <LoadingIndicatorProgress className="h-full w-full bg-background" />}
           <ResizablePanelGroup {...resizablePanelGroupProps}>
             {/* LEFT SIDEPANELS */}
-            {hasLeftPanels ? (
+            {appConfig.showLeftPanel && hasLeftPanels ? (
               <>
                 <ResizablePanel {...resizableLeftPanelProps}>
                   <SidePanelWithServices
@@ -197,7 +197,7 @@ function ViewerLayout({
                 </div>
               </div>
             </ResizablePanel>
-            {hasRightPanels ? (
+            {appConfig.showRightPanel && hasRightPanels ? (
               <>
                 <ResizableHandle
                   onDragging={onHandleDragging}
diff --git a/modes/segmentation/src/index.tsx b/modes/segmentation/src/index.tsx
index d95b679..fa2acb5 100644
--- a/modes/segmentation/src/index.tsx
+++ b/modes/segmentation/src/index.tsx
@@ -6,6 +6,7 @@ import {
   cornerstone,
   extensionDependencies,
   dicomRT,
+  dicomsr,
   segmentation,
   isValidMode,
   onModeEnter as basicOnModeEnter,
@@ -114,7 +115,12 @@ export const modeInstance = {
   extensions: extensionDependencies,
   // Prefer the grid layout hanging protocol when applicable.
   hangingProtocol: ['@ohif/mnGrid'],
-  sopClassHandlers: [ohif.sopClassHandler, segmentation.sopClassHandler, dicomRT.sopClassHandler],
+  sopClassHandlers: [
+    ohif.sopClassHandler,
+    segmentation.sopClassHandler,
+    dicomRT.sopClassHandler,
+    dicomsr.sopClassHandler,
+  ],
 };
 
 /**
diff --git a/platform/ui-next/src/components/Header/Header.tsx b/platform/ui-next/src/components/Header/Header.tsx
index 35964b6..f2cacc2 100644
--- a/platform/ui-next/src/components/Header/Header.tsx
+++ b/platform/ui-next/src/components/Header/Header.tsx
@@ -17,6 +17,7 @@ import NavBar from '../NavBar';
 
 interface HeaderProps {
   children?: ReactNode;
+  className?: string;
   menuOptions: Array<{
     title: string;
     icon?: string;
